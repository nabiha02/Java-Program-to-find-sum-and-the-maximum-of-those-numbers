# Java-Program-to-find-sum-and-the-maximum-of-those-numbers
In this Java progra, we will declare an integer array of size 10, then assign the values as we see fit. We will find the sum of these numbers the user just entered and also find the maximum number from those numbers.
